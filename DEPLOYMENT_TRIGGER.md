# Deployment trigger

Public academic directory and navigation fixes are present on `main` and should be deployed by Railway.

- `/faculties`
- `/institutes-and-centers`
- `/student-portal`
- `/e-learning`
- `/about`

This file intentionally creates a new `main` commit so the Railway production service can pick up the latest application changes.
