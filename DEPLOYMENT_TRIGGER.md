# Deployment trigger

Public academic directory routes are present on `main` and should be deployed by Railway.

- `/faculties`
- `/institutes-and-centers`
- `/e-learning`

This file intentionally exists to create a new `main` commit so the Railway production service can pick up the latest application changes.
